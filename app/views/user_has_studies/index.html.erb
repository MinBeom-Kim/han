
<div class="jumbotron" style="margin-bottom: 3 ">
	<div class="container">
        <h1 style="font-family:inherit">모든 새탁실 사용/예약 리스트</h1>
    </div>
</div>

<div class="container">
<table style="margin: 2%">
  <thead>
    <tr>
      <th>no</th>
      <th>사용자</th>
      <th>역할</th>
      <th>세탁기번호</th>
      <th>세탁기 사용 및 예약 시간</th>
      <th>내용</th>
      <th colspan="7"></th>
    </tr>
  </thead>

  <tbody>
    <% @user_has_studies.each do |user_has_study| %>
      <tr>
        <td><%= user_has_study.user_name? %></td>
        <td><%= user_has_study.user_role? %></td>
        <td><%= Study.find(user_has_study.study_id).study_name %></td>
        <td><%= link_to 'Show', user_has_study %></td>
        <td><%= link_to 'Destroy', user_has_study, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

</div>